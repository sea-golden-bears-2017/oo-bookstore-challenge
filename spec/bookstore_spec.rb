require 'rspec'
require_relative '../bookstore'

describe Book do
  let(:book) { Book.new }

  describe "book"
    it "has an ID" do
      expect(book).to eq()
    end
    it "has a title" do
      expect(book).to eq()
    end
    it "has an author" do
      expect(book).to eq()
    end
    it "has a genre" do
      expect(book).to eq()
    end
    it "has a price" do
      expect(book).to eq()
    end
    it "has a count" do
      expect(book).to eq()
    end
    it "has an isbn" do
      expect(book).to eq()
    end
end
